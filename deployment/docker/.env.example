# Required: Your domain name (must have DNS A record pointing to this server)
DOMAIN=skipbo.example.com

# Optional: Email for LetsEncrypt certificate expiry notifications
# ACME_EMAIL=admin@example.com
